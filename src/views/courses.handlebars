{{#each students}}
    <p>{{this.titulo}}</p>
    <p>{{this.description}}</p>
    <p>{{this.teacherName}}</p>
        {{#each this.students}}
        <p>{{this.name}}</p>
        <p>{{this.age}}</p>
        {{/each}}
{{/each}}